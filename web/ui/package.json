{
  "name": "tracebi-ui",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:echarts": "esbuild echarts-bundle.js --bundle --minify --format=iife --banner:js=\"/*! Third-party libraries bundled and inlined into TraceBi reports: Apache ECharts 6.1.0 (Apache-2.0), zrender 6.1.0 (BSD-3-Clause), tslib 2.3.0 (ISC). Full attribution: NOTICE in the TraceBi distribution. */\" --outfile=../../tracebi/reports/assets/echarts.min.js"
  },
  "dependencies": {
    "@fontsource-variable/inter": "^5.3.0",
    "@tanstack/react-query": "^5",
    "react": "^18",
    "react-dom": "^18",
    "react-markdown": "^9",
    "react-router-dom": "^6",
    "reactflow": "^11",
    "recharts": "^2",
    "remark-gfm": "^4"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4",
    "echarts": "^6.1.0",
    "vite": "^8"
  }
}
