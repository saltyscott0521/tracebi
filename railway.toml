[build]
buildCommand = "pip install '.[reports,dashboard,pipeline,lineage,sql]'"

[deploy]
startCommand = "python -m uvicorn web.api.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/api/health"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
