### Fixed

- The no-JavaScript picture of a chart (what a phone's file preview or an email client shows) now reads like the live chart. A line chart labels every few years instead of all of them on top of each other. Series take plain names ("Median price") and there are no raw column names as axis titles. The chart keeps its own shape instead of sitting in a mostly empty box, and it takes the page's chart palette.
